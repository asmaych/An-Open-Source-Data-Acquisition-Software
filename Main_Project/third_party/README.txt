These libraries are not being used at the moment for CMake purposes.

For development, we will rely on prebuilt libraries to speed up compile times.

On release, the top-level CMakeLists will be modified to build these libraries from source
